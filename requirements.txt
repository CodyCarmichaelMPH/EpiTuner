# EpiTuner - Minimal Requirements
# Only essential dependencies for "Always Works" approach

# Core ML libraries (REQUIRED)
torch>=2.0.0
transformers>=4.35.0
datasets>=2.14.0
peft>=0.6.0

# UI and data processing (REQUIRED)
streamlit>=1.28.0
pandas>=1.5.0

# Optional but recommended
accelerate>=0.24.0  # Faster training
bitsandbytes>=0.41.0  # Quantization (may fail on some systems)

# Note: scipy, nltk, evaluate, rouge-score, tensorboard removed for simplicity